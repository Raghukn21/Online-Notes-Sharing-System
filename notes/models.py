from django.db import models
from django.contrib.auth.models import User

# Application models are now stored in MongoDB via Pymongo in views.py.
# This file is kept minimal as Django requires it.

# Redundant models removed to focus on NoSQL storage.
